/**
 * Form data interface for creating/updating competitions
 */
export interface CompetitionFormData {
  name: string;
  organizer: string;
  date: string;
  description: string;
  result: string;
}
